[gd_scene format=3 uid="uid://b5gd7dkkan5hm"]

[ext_resource type="PackedScene" uid="uid://bdy02rhgd85w2" path="res://Scenes/Wheel.tscn" id="1_cvnsp"]
[ext_resource type="Texture2D" uid="uid://0mhw3xged67m" path="res://Images/Car/Car.png" id="2_6t5aa"]
[ext_resource type="Texture2D" uid="uid://f7qvv1kcwmr3" path="res://Images/Characters/Body.png" id="3_fkybt"]
[ext_resource type="Texture2D" uid="uid://dxrs7mufrjrnf" path="res://Images/Characters/Head.png" id="5_x3wgy"]
[ext_resource type="AudioStream" uid="uid://bq84lwo73rgxr" path="res://Audio/EngineSound.ogg" id="6_x3wgy"]

[sub_resource type="GDScript" id="GDScript_fkybt"]
script/source = "extends RigidBody2D

# --- Wheels & state ---
var wheels: Array = []
@export var max_speed: float = 80.0

# --- Tap-to-drive ---
@export var per_tap_add: float = 0.3
@export var tap_decay: float = 1.2
@export var torque_per_charge: float = 15000
@export var body_tilt_torque: float = -2500
@export var charge_max: float = 2.0
@export var energy_per_tap: int = 5

var pedal_charge: float = 0.0
var energy_points: int = 0

# --- Freeze / input lock ---
var input_locked: bool = false
var frozen: bool = false

@onready var engine_sfx: AudioStreamPlayer = get_node_or_null(\"EngineSFX\") as AudioStreamPlayer

func _ready() -> void:
	add_to_group(\"player\")
	add_to_group(\"input_receivers\") # LevelManager will call lock_input(true)
	add_to_group(\"freezable\")       # LevelManager can call freeze() on this group too

	wheels = get_tree().get_nodes_in_group(\"wheel\")
	for w in wheels:
		if w is RigidBody2D:
			var rb := w as RigidBody2D
			rb.contact_monitor = true
			rb.max_contacts_reported = 4
	_update_energy_ui()

func _unhandled_input(event: InputEvent) -> void:
	if input_locked or frozen:
		return
	if event.is_action_pressed(\"ui_accept\"):
		on_pedal_tap()

func on_pedal_tap() -> void:
	if input_locked or frozen:
		return
	pedal_charge = clamp(pedal_charge + per_tap_add, 0.0, charge_max)
	energy_points += energy_per_tap
	_update_energy_ui()

func _physics_process(delta: float) -> void:
	if frozen:
		# Keep everything stopped while frozen
		pedal_charge = 0.0
		linear_velocity = Vector2.ZERO
		angular_velocity = 0.0
		sleeping = true
		for wheel in wheels:
			if wheel is RigidBody2D:
				var rb := wheel as RigidBody2D
				rb.angular_velocity = 0.0
				rb.linear_velocity = Vector2.ZERO
				rb.sleeping = true
		if engine_sfx:
			engine_sfx.pitch_scale = 1.0
		return

	# Normal driving
	pedal_charge = max(0.0, pedal_charge - tap_decay * delta)

	var grounded := _any_wheel_grounded()
	if pedal_charge > 0.0 and wheels.size() > 0 and grounded:
		var impulse: float = torque_per_charge * pedal_charge * delta * 60.0
		for wheel in wheels:
			if wheel is RigidBody2D:
				var rb := wheel as RigidBody2D
				if rb.angular_velocity < max_speed:
					rb.apply_torque_impulse(impulse)
		if body_tilt_torque != 0.0:
			apply_torque_impulse(body_tilt_torque * delta * 60.0)

	if engine_sfx:
		var target := 2.0 if (pedal_charge > 0.0 and grounded) else 1.0
		engine_sfx.pitch_scale = lerp(engine_sfx.pitch_scale, target, 2.0 * delta)

func _any_wheel_grounded() -> bool:
	for wheel in wheels:
		if wheel is RigidBody2D:
			if (wheel as RigidBody2D).get_contact_count() > 0:
				return true
	return false

func _update_energy_ui() -> void:
	var p := get_parent()
	if p and p.has_method(\"update_energy_UI\"):
		p.update_energy_UI(energy_points)

# --- Called by LevelManager ---

func lock_input(v: bool) -> void:
	input_locked = v

func freeze() -> void:
	frozen = true
	pedal_charge = 0.0
	linear_velocity = Vector2.ZERO
	angular_velocity = 0.0
	sleeping = true
	for wheel in wheels:
		if wheel is RigidBody2D:
			var rb := wheel as RigidBody2D
			rb.angular_velocity = 0.0
			rb.linear_velocity = Vector2.ZERO
			rb.sleeping = true
	if engine_sfx:
		engine_sfx.pitch_scale = 1.0
"

[sub_resource type="CircleShape2D" id="CircleShape2D_3smsa"]
radius = 65.00769

[node name="player" type="RigidBody2D" unique_id=640640380 groups=["player"]]
gravity_scale = 3.5
script = SubResource("GDScript_fkybt")
torque_per_charge = 8000.0

[node name="wheelholder" type="PinJoint2D" parent="." unique_id=395916683]
position = Vector2(-131, 68)
node_a = NodePath("..")
node_b = NodePath("wheel")
softness = 2.58

[node name="wheel" parent="wheelholder" unique_id=1679211905 instance=ExtResource("1_cvnsp")]
position = Vector2(-2, 0)

[node name="wheelholder2" type="PinJoint2D" parent="." unique_id=210201304]
position = Vector2(134, 70)
node_a = NodePath("..")
node_b = NodePath("wheel2")
softness = 2.58

[node name="wheel2" parent="wheelholder2" unique_id=1908700164 instance=ExtResource("1_cvnsp")]
position = Vector2(0, -1)

[node name="Sprite2D" type="Sprite2D" parent="." unique_id=1209334294]
position = Vector2(-28, -60)
texture = ExtResource("3_fkybt")

[node name="Head" type="RigidBody2D" parent="." unique_id=1472693749 groups=["player"]]
position = Vector2(-30, -144)
mass = 0.001
lock_rotation = true

[node name="Sprite2D" type="Sprite2D" parent="Head" unique_id=1923390411]
position = Vector2(-1, 0)
texture = ExtResource("5_x3wgy")

[node name="CollisionShape2D" type="CollisionShape2D" parent="Head" unique_id=1319094092]
position = Vector2(1, -1)
shape = SubResource("CircleShape2D_3smsa")

[node name="HeadSpring" type="PinJoint2D" parent="Head" unique_id=1613036404]
position = Vector2(0, -1)
node_a = NodePath("..")
node_b = NodePath("../..")

[node name="Car" type="Sprite2D" parent="." unique_id=764152539]
position = Vector2(25, -20)
texture = ExtResource("2_6t5aa")

[node name="CollisionPolygon2D" type="CollisionPolygon2D" parent="." unique_id=1030341598]
position = Vector2(25, -20)
polygon = PackedVector2Array(-222, -45, -222, 73, -209, 100, 194, 101, 217, 83, 216, -10, 6, -61, -200, -59)

[node name="GameOverTimer" type="Timer" parent="." unique_id=1542665962]
wait_time = 5.0

[node name="EngineSFX" type="AudioStreamPlayer" parent="." unique_id=1682945267]
stream = ExtResource("6_x3wgy")
autoplay = true

[node name="SlopeLeft" type="RayCast2D" parent="." unique_id=395632126]
position = Vector2(-133, 12)
target_position = Vector2(0, 110)

[node name="SlopeRight" type="RayCast2D" parent="." unique_id=2056123223]
position = Vector2(138, 15)
target_position = Vector2(-5, -110)

[connection signal="timeout" from="GameOverTimer" to="." method="_on_game_over_timer_timeout"]
